services:
  budget-tracker:
    build: .
    container_name: budget-tracker
    restart: unless-stopped
    env_file: .env
    ports:
      - "${PORT:-3000}:3000"
    volumes:
      - budget-data:/app/data

volumes:
  budget-data:
